# Megent Policy Workspace

This repository holds policy packs for Megent.

## Layout

- `policies/` for YAML policy packs
- `examples/` for usage examples
- `docs/` for short notes and references

## Example

A policy pack should stay simple and focused on direct tool names or wildcard matching.
